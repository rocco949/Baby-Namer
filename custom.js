const possible_first_names = []
const possible_last_names = []

function changer() {	
	let firstN = Math.floor(Math.random() * possible_first_names.length)
	let lastN = Math.floor(Math.randomm() * possible_last_names.length)
	let names = [document.getElementById(first_name, last_name)]

	names[0] = firstN;
	names[1] = lastN;
		
	}